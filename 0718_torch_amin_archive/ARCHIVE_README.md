# 0718 Torch A-min Archive

This directory stores the retired Torch CV-AL A-min attempt.

It is kept for reference only:

- `torch_pinn/`: previous A-min package.
- `reynold_pinn_torch.py`: previous training entry.
- `compare_fem_pinn_*_torch.py`: previous evaluation entries.
- `res/`: previous full-run outputs and diagnostics.
- `output_torch_smoke_active/`: previous smoke output.
- `tests/test_torch_cv_al.py`: previous unit tests.

The active new implementation starts in `../torch_xpinn/` and `../reynold_xpinn.py`.
